{
  "name": "gifbattles",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "npm run build && playwright test",
    "test:smoke": "npm run build && playwright test tests/smoke.spec.ts",
    "test:e2e": "npm run build && playwright test tests/e2e"
  },
  "dependencies": {
    "@sentry/nextjs": "^10.41.0",
    "@upstash/redis": "^1.36.2",
    "@vercel/blob": "^2.3.0",
    "next": "^14",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^20",
    "@types/react": "^18",
    "typescript": "^5"
  }
}
